---
description: Loads the project's essential context to start a task aligned with conventions.
---

1. Read `CLAUDE.md` to understand the role and general rules.
2. Read `docs/CONVENTIONS.md` to refresh mandatory code patterns.
3. Read `docs/ARCHITECTURE.md` to get the system's macro vision.
4. Confirm to user "Context loaded: [1-line architecture summary]".

> Use this workflow at the start of every complex session to ensure consistency.
